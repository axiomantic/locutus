[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: Fast hermetic unit tests (protocol, binary, packaging)
    llm: Live LLM integration tests (Ollama or cloud API)
    e2e: Multi-agent end-to-end integration tests
